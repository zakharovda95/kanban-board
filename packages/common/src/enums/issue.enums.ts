export enum EIssueEvent {
  CREATE = 'issue.create',
  CREATED = 'issue.created',
  UPDATE = 'issue.update',
  UPDATED = 'issue.updated',
  DELETE = 'issue.delete',
  DELETED = 'issue.deleted',
  MOVE = 'issue.move',
  MOVED = 'issue.moved',
}
