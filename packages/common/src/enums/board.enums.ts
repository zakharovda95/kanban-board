export enum EBoardEvent {
  JOIN = 'board.join',
  LEAVE = 'board.leave',
  CREATE = 'board.create',
  CREATED = 'board.created',
  UPDATE = 'board.update',
  UPDATED = 'board.updated',
  DELETE = 'board.delete',
  DELETED = 'board.deleted',
  MOVE = 'board.move',
  MOVED = 'board.moved',
}
